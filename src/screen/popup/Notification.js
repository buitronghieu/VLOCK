import {Text,StyleSheet} from 'react-native';
import Modal from './Modal';

export default class Notificaiton extends Modal{
    renderMain(){
        return(
            <Text>adbc</Text>
        )
    }
}